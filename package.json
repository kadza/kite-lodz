{
  "name": "jeziorsko",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server",
    "dev": "eleventy --serve --port=8080",
    "dev-simple": "eleventy && live-server dist --port=8080",
    "build": "eleventy",
    "watch": "eleventy --watch",
    "optimize": "node scripts/optimize-images.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "live-server": "^1.2.2",
    "sharp": "^0.33.0"
  }
}
